import { RegisterForm } from "@/components/auth/RegisterForm";

export default function page() {
  return (
    <div className="flex items-center justify-center h-screen mt-10 px-6">
      <RegisterForm/>
    </div>
  )
}